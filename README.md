# python-pdfimgextract
How to use:

pip install -r requeriments.txt

python ./pdfimgextract.py [INPUT_PDF] [OUTPUT_DIR] [NUMBER_OF_PROCESS_IN_PARALLEL]

or use the opitional flags: 
--input | -i
--output | -o
--parallelism | -p
